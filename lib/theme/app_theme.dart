import 'package:flutter/material.dart';

class AppColors {
  // Main Accents & Backgrounds
  static const Color primary = Color(0xFF6366F1);      // Indigo (Vibrant Accent)
  static const Color secondary = Color(0xFF0F172A);    // Slate Blue/Dark Gray (For primary text/headings instead of harsh pitch black)
  static const Color tertiary = Color(0xFF64748B);     // Muted Slate (For subtexts/descriptions)
  static const Color background = Color(0xFFF8FAFC);   // Off-white/Soft Slate (Easier on the eyes than blinding pure white)
  static const Color surface = Color(0xFFFFFFFF);      // Pure White (For cards, containers, buttons text)

  // Status Colors
  static const Color fail = Color(0xFFEF4444);         // Soft Red (Modern Error)
  static const Color success = Color(0xFF10B981);      // Emerald Green (Modern Success)
}